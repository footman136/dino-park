package dinopark.plants;

type HarvestRequest {
  EntityId harvester = 1;
  float resources_need = 2;
}

type HarvestResponse {
  float resources_taken = 1;
}

component Harvestable {
  id = 1601;
  command HarvestResponse harvest(HarvestRequest);
}
